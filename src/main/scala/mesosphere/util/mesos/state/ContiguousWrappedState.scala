/*
package mesosphere.util.mesos.state

import mesosphere.util.mesos.state.collection.contiguous._
import org.apache.mesos.state.State

class ContiguousWrappedState[T](
    state: State,
    formatter: ValueFormatter[T]) extends WrappedState[T] {

  def makeList(name: String): ContiguousList[T]
  def makeSet(name: String): ContiguousSet[T]
  def makeMap(name: String): ContiguousMap[T]

}
*/
